# ─── Secrets (NEVER COMMIT) ───────────────────────────────
.env
*.env
**/config/.env
.streamlit/secrets.toml
check_followup.py
seed_postgres.py

# ─── Build output (rebuilt locally before deploying) ──────
# NOTE: web/dist/ must be committed because Railway only runs Python (no Node build step)
# Rebuild locally with: cd web && VITE_API_URL=/api/v1 npx vite build

# ─── Python ───────────────────────────────────────────────
__pycache__/
*.pyc
*.pyo
*.pyd
.Python

# ─── Virtual environments (large, machine-specific) ───────
.venv/
.venv_codex/
**/.venv/
**/.venv_codex/
venv/
env/

# ─── Node ─────────────────────────────────────────────────
node_modules/

# ─── Databases (local SQLite, may contain customer PII) ───
*.db
*.db.bak
*.bak
*.sqlite
*.sqlite3

# ─── Runtime data (lead PII, backups, processed exports) ──
sales_lead_system/data/
sales_lead_system/logs/
sales_lead_system/exports/
sales_lead_system/outputs/
outputs/

# ─── Logs ─────────────────────────────────────────────────
*.log

# ─── Editor / OS ──────────────────────────────────────────
.vscode/
.idea/
.DS_Store
Thumbs.db

# ─── Claude / agent local state ───────────────────────────
.claude/
