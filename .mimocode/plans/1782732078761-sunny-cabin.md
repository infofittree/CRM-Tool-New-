# Plan: Country Autocomplete in Data Entry

## Change
Replace the plain text Country input with a searchable autocomplete. Typing filters the list (e.g. "aus" shows Australia, Austria). Selecting a country fills the field. Free-text still allowed if no match.

## File
- `web/src/pages/DataEntry.tsx` — Replace FormField for Country with inline autocomplete component using a static country list + client-side filtering

## Verification
1. Type "aus" → dropdown shows matching countries
2. Select one → field filled
3. Clear and type free text → accepted
