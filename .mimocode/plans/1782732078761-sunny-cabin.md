# Plan: Convert Task Drawer to Centered Modal

## Current State
The task detail panel is a right-side drawer (`fixed top-0 right-0`) that slides in from the right edge.

## Desired State
A centered modal window (like the ActivityWizard) that appears in the middle of the screen.

## Change
**File:** `web/src/pages/Tasks.tsx` — line 214

Change the container div from:
```
fixed top-0 right-0 z-50 h-full w-full max-w-lg bg-white shadow-[var(--shadow-modal)] border-l border-border/40 flex flex-col animate-slide-in-right
```

To:
```
fixed inset-0 z-50 flex items-center justify-center bg-black/25 backdrop-blur-[2px] animate-fade-in
```

And change the inner content div from:
```
fixed top-0 right-0 z-50 h-full w-full max-w-lg bg-white shadow-[var(--shadow-modal)] border-l border-border/40 flex flex-col animate-slide-in-right
```

To:
```
bg-white rounded-2xl shadow-[var(--shadow-modal)] w-full max-w-lg mx-4 max-h-[90vh] overflow-y-auto animate-scale-in
```

Remove the separate backdrop div since the outer container now handles it.

## Files to modify
- `web/src/pages/Tasks.tsx` — container classes only

## Verification
1. Click a task → centered modal appears
2. Click backdrop → modal closes
3. Scroll inside modal works
4. Complete Task button still works
5. All existing functionality preserved
