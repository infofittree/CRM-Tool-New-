# Plan: Simplify Revision Request Form

## Change
Replace the complex revision form (6 structured fields + dropdown + textarea) with just two fields:

1. **Customer Feedback** (required textarea) — What did the customer say?
2. **What do you need from procurement?** (required textarea) — What changes are needed?

Two buttons: Cancel + Send Revision Request.

## Files to Modify
- `web/src/components/InquiryWorkflowModal.tsx` — Simplify revision form in the "revision" view
- `sales_lead_system/api/schemas.py` — Simplify RevisionCreate schema (keep `customer_feedback` and `additional_requirements`, remove `reason` dropdown and structured fields)

## Verification
1. Click Request Revision → form shows only 2 textareas + Send button
2. Submit works
3. Backend saves correctly
