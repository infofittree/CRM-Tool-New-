# Plan: Fix Inquiry Creation Button Visibility

## Root Cause
Line 38 defines `isProc = user?.role === "Admin" || user?.role === "Manager" || user?.role === "Procurement"`. The "New Inquiry" button check on line 108 uses `!isProc`, which excludes Admin and Manager (since they're in `isProc`).

## Fix
Change line 108 from `{!isProc && (` to `{user?.role !== "Procurement" && (`

## File
- `web/src/pages/InquiryPortal.tsx` — one line change
